<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.materialdesign">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.materialdesign.Activity.RegisterActivity"
            android:parentActivityName="com.example.materialdesign.Activity.LoginActivity"/>
        <activity android:name="com.example.materialdesign.Activity.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.materialdesign.Activity.InformasiActivity" />
        <activity android:name="com.example.materialdesign.Activity.ProfileActivity" />
        <activity android:name="com.example.materialdesign.Activity.LoginActivity" />
        <activity android:name="com.example.materialdesign.Activity.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>